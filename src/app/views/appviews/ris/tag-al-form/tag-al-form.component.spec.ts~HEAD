import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TagAlFormComponent } from './tag-al-form.component';

describe('TagAlFormComponent', () => {
  let component: TagAlFormComponent;
  let fixture: ComponentFixture<TagAlFormComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TagAlFormComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TagAlFormComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
