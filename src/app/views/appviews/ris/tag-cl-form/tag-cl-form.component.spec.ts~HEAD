import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TagClFormComponent } from './tag-cl-form.component';

describe('TagClFormComponent', () => {
  let component: TagClFormComponent;
  let fixture: ComponentFixture<TagClFormComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TagClFormComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TagClFormComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
